# Flutter 위젯(Widget)의 기초 



## 1. Flutter의 핵심 철학: "Every component is a Widget"

Flutter에서 화면에 보이는 모든 UI 요소(텍스트, 이미지, 버튼 등)뿐만 아니라, 화면의 구조를 잡는 레이아웃(Padding, Center, Row, Column)과 눈에 보이지 않는 설정(Theme, GestureDetector)까지 **모든 것이 위젯(Widget)**으로 이루어져 있습니다.

Flutter 앱을 빌드한다는 것은 마치 레고 블록을 조립하듯, 작은 위젯들을 결합하여 거대한 **위젯 트리(Widget Tree)**를 만드는 과정과 같습니다.

---

## 2. 위젯의 두 가지 핵심 유형: StatelessWidget vs StatefulWidget

Flutter 위젯은 상태(State)의 유무에 따라 크게 두 가지로 분류됩니다. 이 구조를 이해하는 것이 Flutter 개발의 첫걸음입니다.

### 2.1 StatelessWidget (정적 위젯)
* **개념:** 한 번 화면에 그려지면 **절대로 변하지 않는** 정적인 위젯입니다.
* **특징:** 자체적인 '상태(데이터)'를 가지지 않으며, 외부에서 전달받은 데이터(설정값)로만 화면을 그립니다. 화면을 다시 그릴 필요가 없기 때문에 가볍고 효율적입니다.
* **예시:** 단순 텍스트(`Text`), 아이콘(`Icon`), 고정된 이미지(`Image`) 등.

### 2.2 StatefulWidget (동적 위젯)
* **개념:** 유저의 상호작용, 데이터 변경 등에 따라 **화면의 내용이 동적으로 변하는** 위젯입니다.
* **특징:** 위젯 내부에 변경 가능한 **상태(State)**를 가지고 있습니다. 데이터가 바뀌면 `setState()` 함수를 호출하여 화면을 내부적으로 다시 그려(Rebuild) 줍니다.
* **예시:** 체크박스(`Checkbox`), 입력창(`TextField`), 숫자가 올라가는 카운터 앱 등.

---

## 3. 핵심 기본 위젯 가이드

초기 UI 구성 시 반드시 알아야 할 필수 기본 위젯 4가지 카테고리입니다.

### ① 구조 및 레이아웃 위젯
* **Scaffold:** 앱의 기본 뼈대가 되는 위젯으로, 상단 바(`AppBar`), 바디(`Body`), 하단 네비게이션 바 등을 배치할 수 있는 지지대 역할을 합니다.
* **Center:** 자식(Child) 위젯을 화면의 정중앙에 정렬합니다.
* **Container:** 레이아웃의 기본 상자입니다. 배경색, 크기(너비/높이), 테두리(Border), 여백(Margin/Padding)을 설정할 때 가장 많이 쓰입니다.

### ② 다중 위젯 배치 레이아웃
* **Row:** 여러 개의 위젯을 **가로(수평)** 방향으로 나란히 배치할 때 사용합니다.
* **Column:** 여러 개의 위젯을 **세로(수직)** 방향으로 나란히 배치할 때 사용합니다.

### ③ 기본 컴포넌트 위젯
* **Text:** 화면에 문자열을 표시하는 위젯입니다. `style: TextStyle(...)`을 통해 글자 크기, 색상, 두께 등을 지정합니다.
* **Icon:** 플러터가 기본 제공하는 머티리얼 아이콘을 쉽게 그려주는 위젯입니다. (`Icons.home`, `Icons.star` 등)

---
